---
description: Do not add Cursor co-author or commit trailers
alwaysApply: true
---

Never add `Co-authored-by: Cursor`, `Made-with: Cursor`, or any Cursor
attribution trailer to git commits or pull requests.

When committing, use plain messages only (`git commit -m "..."`). Do not pass
`--trailer` for Cursor attribution.
